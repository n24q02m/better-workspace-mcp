# Time Tool — Full Documentation

## Overview
Local date/time/timezone helpers. No Google account required.

## Actions
- **getCurrentDate** — the current date.
- **getCurrentTime** — the current time.
- **getTimeZone** — the system timezone.

The `account` param is accepted for signature parity but ignored (time needs no account).
